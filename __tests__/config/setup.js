// Mocked to prevent errors when using `useNavigation` hook.
jest.mock('@react-navigation/native');
// Mocked to prevent annoying warning because of useNativeDriver
jest.mock('react-native/Libraries/Animated/src/NativeAnimatedHelper');
